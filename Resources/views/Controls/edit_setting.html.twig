{% if fox_setting_enabled('fox_admin_live_settings') %}
<a href="{{ url('fox_admin_setting_edit', {'name': setting, 'type': type}) }}" class="btn btn-default pull-right" title="Edit {{ setting }}">
    <span class="glyphicon glyphicon-wrench"></span>
</a>
{% endif %}
